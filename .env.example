# FinGuard AI - Environment Configuration

# =============================================================================
# API Keys (REQUIRED)
# =============================================================================
# Get your free Google Gemini API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here


# =============================================================================
# AI Service Configuration
# =============================================================================
# Embedding model for generating document vectors
EMBEDDING_MODEL=models/gemini-embedding-001

# LLM model for generating responses
# Options: models/gemini-2.5-flash (faster, cheaper) or models/gemini-2.5-pro (better quality)
LLM_MODEL=models/gemini-2.5-flash

# LLM temperature (0.0 = deterministic, 1.0 = creative)
TEMPERATURE=0.1

# Maximum tokens to generate in response
MAX_TOKENS=500


# =============================================================================
# RAG Configuration
# =============================================================================
# Number of documents to retrieve for context
# Higher = more context but slower and more expensive
TOP_K_RESULTS=3

# Document chunking settings
CHUNK_SIZE=500
CHUNK_OVERLAP=50


# =============================================================================
# Vector Store Configuration
# =============================================================================
# Directory for ChromaDB persistence
CHROMA_PERSIST_DIR=./chroma_db

# Collection name for storing document embeddings
COLLECTION_NAME=fintech_policies


# =============================================================================
# Observability Configuration
# =============================================================================
# Cost tracking (per 1000 tokens)
# Gemini pricing as of Feb 2026
GEMINI_INPUT_COST=0.00001
GEMINI_OUTPUT_COST=0.00003


# =============================================================================
# Application Settings
# =============================================================================
# Application name
APP_NAME=FinGuard AI Support

# Environment (development, staging, production)
ENVIRONMENT=development
